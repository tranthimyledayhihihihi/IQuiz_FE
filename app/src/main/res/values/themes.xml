<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Theme mặc định -->
    <style name="Theme.FE" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_primary</item>
        <item name="colorPrimaryVariant">@color/purple_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@color/purple_dark</item>
    </style>

    <!-- Theme không có ActionBar (cho các màn hình con) -->
    <style name="Theme.FE.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_primary</item>
        <item name="colorPrimaryVariant">@color/purple_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@color/purple_dark</item>
    </style>

</resources>
